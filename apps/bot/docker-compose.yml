services:
  bot:
    build:
      context: ../../
      dockerfile: apps/bot/Dockerfile
    restart: unless-stopped
    env_file: ../../.env
    volumes:
      - bot-data:/app/apps/bot/data

volumes:
  bot-data:
